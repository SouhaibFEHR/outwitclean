import React from 'react';

// This component's functionality (the old SVG astronaut) has been fully replaced
// by the astronaut logic integrated into InteractiveAstronautScene.jsx.
// The old SVG astronaut and its specific orbiting animation are no longer needed.
// This component now returns null to ensure it doesn't render anything.

const FloatingAstronaut = () => {
  return null; // Effectively removing the old SVG astronaut
};

export default FloatingAstronaut;